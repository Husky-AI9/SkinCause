# SkinCause mobile proof shell

This package proves that the contracts, API client, domain wording, design tokens, and platform image shape compile without importing web or server code. The production Expo Router screens can wrap these interfaces with Expo Camera/ImagePicker and SecureStore without changing `/api/v1`.
