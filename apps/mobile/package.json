{
  "name": "@skincause/mobile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@skincause/api-client": "*",
    "@skincause/contracts": "*",
    "@skincause/design-tokens": "*",
    "@skincause/domain": "*"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  }
}
